import React from 'react';
import { StyledInput } from './SearchInput.styles';

const SearchInput = () => {
    return <StyledInput type="text" placeholder="Search exercises..." />;
};

export default SearchInput;
