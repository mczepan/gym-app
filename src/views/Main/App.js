import React from 'react';
import MainTemplate from 'templates/MainTemplate';

export const App = () => {
    return (
        <MainTemplate>
            <section>APP</section>
        </MainTemplate>
    );
};

export default App;
